<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.uielements">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UIELEMENTS">
        <activity android:name=".NotificationDestinationActivity"></activity>
        <activity android:name=".Dialogs" />
        <activity android:name=".image3" />
        <activity android:name=".image2" />
        <activity android:name=".image1" />
        <activity android:name=".Album" />
        <activity android:name=".QuedSongs" />
        <activity android:name=".Songs" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>